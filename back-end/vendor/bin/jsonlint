#!/usr/bin/env sh
dir=$(cd "${0%[/\\]*}" > /dev/null; cd "../seld/jsonlint/bin" && pwd)
if [ -d /proc/cygdrive ]; then
    case $(which php) in
        $(readlink -n /proc/cygdrive)/*)
            dir=$(cygpath -m "$dir");
            ;;
    esac
fi
"${dir}/jsonlint" "$@"
